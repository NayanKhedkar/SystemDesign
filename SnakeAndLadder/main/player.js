export default class Player {
    // how many game he played // how many game he lost // rank the player id required
    constructor(name, id) {
        this.name = name
        this.id = id
    }
    // get name() {
    //     return this.name
    // }
}